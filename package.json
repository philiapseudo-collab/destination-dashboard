{
  "name": "destination-dashboard-root",
  "version": "1.0.1",
  "private": true,
  "description": "Destination Manager Dashboard - Railway Deployment",
  "scripts": {
    "install": "cd web && npm install",
    "build": "cd web && npm run build",
    "start": "cd web/.next/standalone && HOSTNAME=0.0.0.0 node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
